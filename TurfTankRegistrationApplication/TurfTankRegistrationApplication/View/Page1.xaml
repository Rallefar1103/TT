<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TurfTankRegistrationApplication.ViewModel"
             x:Class="TurfTankRegistrationApplication.View.Page1">
    <ContentPage.BindingContext>
        <local:ViewModel1/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Turf Tank Registration Application!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Label Text="The press five calls all layers of the application, try it out!"/>
            <Label Text="To run the unittests, please go to XXX"/>
            <Button Text="PressForFive" Command="{Binding PressForFiveCommand}"/>
            <Label Text="{Binding ShowFive}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>